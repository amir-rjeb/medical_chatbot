# Ignorer tous les fichiers PDF
*.pdf

# Autres exemples (optionnels) :
# Ignorer les PDF dans un dossier spécifique (ex: "downloads/")
downloads/*.pdf

# Ignorer les fichiers temporaires (optionnel)
*.tmp